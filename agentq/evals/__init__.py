"""Evaluation-only fixtures, captures, judgments, and replay for agentq.

This package is never imported by the installed ``agentq`` runtime. It holds
the authored fixture builders and judgment drafts, the strict wire codecs and
content-addressed store, the developer CLI (build/capture/replay/catalog/
evaluate/smoke/import-rows/fetch-checkouts), and the evaluator that scores
delivered evidence against independently authored labels.
"""
